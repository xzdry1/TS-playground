let user: object = {
    name: 'dry',
    age: 18
}

console.log(user);


let arr: [number, number, number];

arr = [1, 2, 3]
console.log(arr);

// let size = ['L', 'M', 'S'];
enum Size { L, M, S };
console.log(Size.L);